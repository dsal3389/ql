# install

=== "pip"
    ```console
    pip3 install pydantic-graphql
    ```

=== "pipenv"
    ```console
    pipenv install pydantic-graphql
    ```

=== "poetry"
    ```console
    poetry add pydantic-graphql
    ```
